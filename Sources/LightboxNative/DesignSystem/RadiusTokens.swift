import Foundation

enum RadiusTokens {
    static let small: CGFloat = 5     // chips, checkbox, selection rectangles
    static let control: CGFloat = 9   // buttons, popover rows, list rows
    static let card: CGFloat = 10     // gallery cards, folder tiles, comparison panes
    static let panel: CGFloat = 16    // large floating panels
    static let capsule: CGFloat = 999
}
